import React from "react"
import "./../SocMed.css"

function SocMed(props) {
    return (
        <>
            <a href="#" className={`fa ${props.className}`}></a>
        </>
    )
}

export default SocMed